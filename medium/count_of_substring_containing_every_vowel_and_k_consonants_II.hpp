#include "../imports.hpp"

static inline bool isVowel(const char c) {
    return (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u');
}

class Solution {
public:
    long long countOfSubstrings(string word, int k) {
        int count = 0;
        int wordSize = (int)word.size();
        for (int i = 0, j = 0; j < wordSize; ++j) {

        }
        return count;
    }
};