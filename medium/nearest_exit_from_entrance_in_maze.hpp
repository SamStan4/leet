#pragma once

#include "./../imports.hpp"

class Solution
{
public:

    int nearestExit(vector<vector<char>>& maze, vector<int>& entrance)
    {
        queue<pair<int, int>> go_to_queue;

        go_to_queue.push({entrance[0], entrance[1]});

        
    }
};