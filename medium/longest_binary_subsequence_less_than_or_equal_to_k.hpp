#pragma once

#include "./../imports.hpp"

class Solution {
public:
  int longestSubsequence(string s, int k) {
    const int n = static_cast<int>(s.size());
    int zeros = 0;
    int ones = 0;
    long long value = 0;
    long long power = 0;

    for (int i = 0; i < n; ++i) {
    }
  }
};