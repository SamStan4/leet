#pragma once

#include "./../imports.hpp"

class Solution
{
public:
    bool lemonadeChange(vector<int>& bills)
    {
        int fives = 0, tens = 0;

        for (int i = 0; i < bills.size(); ++i)
        {
            
        }    
    }
};